{
  "name": "svg-boundings",
  "version": "1.0.0",
  "description": "Get bounding rect of SVG shape elements. Shape elements includes <circle>, <ellipse>, <line>, <path>, <polygon>, <polyline>, <rect>. This module doesn't return the exact bounding rect for all the shape elements. However, it returns the same result as the browser does, assuming the origin point is the top left corner of the SVG document.",
  "main": "index.js",
  "scripts": {
    "test": "mocha"
  },
  "keywords": [
    "SVG",
    "Bounding",
    "rect"
  ],
  "author": "Ye Hao",
  "license": "ISC",
  "dependencies": {
    "cheerio": "~0.19.0",
    "mocha": "~2.3.4",
    "should": "~8.0.2"
  }
}
